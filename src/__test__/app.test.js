/* eslint-disable no-undef */
import chai from 'chai';

const { expect } = chai;

describe('Sample Test', () => {
  it('test if 1=1', () => {
    const one = 1;
    expect(one).to.equal(1);
  });
});
